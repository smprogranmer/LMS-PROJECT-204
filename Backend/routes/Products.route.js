// import express from "express";
// import { cloudinaryTest,getOneProduct,getAllProducts,deleteProducts,upgradeProducts } from"../controllers/Products.controllers.js"
// // import isAuthenticatedUser from"../middlewares/auth.js"
// import upload from"../middlewares/uploadFile.js"
// // const  authorizeRoles = require('../middlewares/auth')
// const Router = express.Router();

// // Router.get('/products',isAuthenticatedUser,getAllProducts)
// Router.get("/products", getAllProducts);
// Router.get("/products/:id", getOneProduct);
// Router.post("/products/new", upload.array("images", 5), cloudinaryTest);
// Router.delete("/products/:id", deleteProducts);
// Router.put("/products/:id", upgradeProducts);

// export default Router;
